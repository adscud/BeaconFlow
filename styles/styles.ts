import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  flex: {
    flex: 1,
  },
  padding: {
    padding: 18,
  },
  cardContainer: {
    position: "absolute",
    left: 0,
    top: 0,
    right: 0,
    height: "30%",
  },
});
